import java.sql.Date;
import java.util.TimeZone;
//import java.util.Date;

// not in use in now a days -------------
public class dateclass {
    public static void main(String[] args) {
        Date d = new Date(0);
        System.out.println(d);
        System.out.println(d.getTime());
    }
}
